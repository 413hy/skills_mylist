# 自建 skills 调用自测报告

日期：2026-06-06

范围：仅测试 `001-016` 自建优化 skills。`101-114` 是推荐搭配安装的第三方/本机已有 skills 编号，只在 README 中作为索引说明，不随本仓库上传。

测试方式：逐个读取对应 `SKILL.md`、`references/workflow.md`、`references/operating-contract.md`、`references/scenario-validation.md`，用一个代表性真实使用场景进行 dry-run 调用。判断重点不是“文件存在”，而是该 skill 在场景中是否会产出符合目标的结果、是否会要求真实场景自测、是否避免只测接口或只给空泛建议。

## 结论

16 个自建 skills 均通过代表性调用自测。全部 skill 都具备：

- 明确触发场景和输入要求。
- 硬性执行契约：先理解、再执行、失败要复测。
- 真实场景自测门槛：交付代码、demo、脚本、Agent、PR、同步结果或最终产品功能时，必须拿用户真实会执行的操作样例验证。
- 输出契约：说明产出、场景自测、未测项和剩余风险。

## 逐项自测

### 1. `001-pr-check-commit-pr`

需求理解：用于提交前收口，目标不是简单 `git commit`，而是确认本次变更真实可用后再提交、推送、发 PR。

调用场景：新增后台登录模块，用户要求“检查一下没问题就提交并发 PR”。

按 skill 期望输出：

- 先读取 git 状态、diff、相关需求和测试命令。
- 建立变更范围镜像：登录页、登录接口、session、退出登录。
- 跑 lint/test/build。
- 用测试账号从浏览器入口登录，覆盖正确密码、错误密码、刷新后会话、退出登录、受保护页面访问。
- 通过后提交、推送、准备 PR 描述。

自测结论：通过。该 skill 会阻止“接口 200 就提交”的低质量收口，要求真实用户路径验证。

### 2. `002-user-story-from-materials`

需求理解：用于把零散材料整理成可开发、可测试的 User Story。

调用场景：材料只有“运营人员需要能登录后台管理订单；失败时要提示原因”。

按 skill 期望输出：

- 生成角色、目标、价值。
- 补齐主流程、异常流程、范围外事项和待确认问题。
- 每条验收标准配一个可执行测试样例，例如正确登录、密码错误、账号禁用、会话过期。

自测结论：通过。该 skill 不会只润色原文，而会把故事整理成开发和 QA 都能使用的结构。

### 3. `003-sync-doc-to-target-system`

需求理解：用于把文档、卡片或故事同步到目标系统，并验证目标系统中字段真实一致。

调用场景：把登录 User Story 同步到 Jira。

按 skill 期望输出：

- 先提取源文档字段和目标系统字段映射。
- 同步后检查目标 issue 的标题、描述、验收标准、优先级、负责人、链接和附件。
- 输出目标链接、差异和未能同步字段。

自测结论：通过。该 skill 明确要求同步后回查目标对象，不会只报告“同步接口成功”。

### 4. `004-cross-system-alignment`

需求理解：用于对齐代码、需求、任务系统、外部系统之间的一致性。

调用场景：需求文档写“登录失败 5 次锁定账号”，代码只提示错误，Jira 状态是 Done。

按 skill 期望输出：

- 列出权威来源顺序。
- 抽取需求、代码行为、任务状态。
- 用测试账号连续输错密码 5 次，验证系统是否锁定。
- 输出差异表和修正建议。

自测结论：通过。该 skill 会用真实行为验证文字差异，而不是只比较文档表述。

### 5. `005-step-by-step-guide`

需求理解：用于一步一步指导用户完成任务，每轮只给一个下一步。

调用场景：用户说“登录失败了，你一步一步带我排查”。

按 skill 期望输出：

- 第一轮只要求用户执行一个动作，例如打开开发者工具后尝试登录一次。
- 明确需要回传 Network 中登录请求状态码和响应。
- 等用户反馈后再判断下一步。

自测结论：通过。该 skill 会限制输出节奏，避免一次性倾倒大量排查步骤。

### 6. `006-ai-summary-handoff`

需求理解：用于把长会话压缩成可交接给另一个 AI 或 Codex session 的 Prompt。

调用场景：登录模块开发到一半，需要交给另一个窗口继续。

按 skill 期望输出：

- 当前目标、已完成文件、未完成问题、失败日志、测试账号、已跑测试。
- 下一步建议和验收标准。
- 保留安全、隐私和授权边界。

自测结论：通过。该 skill 能产出可执行交接，而不是泛泛总结“继续完善登录功能”。

### 7. `007-clarify-before-demo`

需求理解：用于完整 demo 交付前先澄清需求，再构建可运行并经过真实场景自测的 demo。

调用场景：用户要求“做一个带登录的后台管理 demo”。

按 skill 期望输出：

- 先只问一个关键问题，例如“后台的核心业务对象是什么？”
- 信息足够后生成需求镜像并等待确认。
- 交付 demo 前预置测试账号，用浏览器验证登录、错误密码、会话保持、退出登录和受保护页面。

自测结论：通过。该 skill 明确拒绝碎片代码交付，并要求完整功能体验自测。

### 8. `008-socratic-concept-learning`

需求理解：用于通过苏格拉底式提问帮助用户理解概念。

调用场景：用户想理解 Cookie、Session、JWT 的区别。

按 skill 期望输出：

- 先给低术语密度的整体地图。
- 一次只问一个诊断问题，例如“服务端如何知道第二次请求还是同一个用户？”
- 根据用户回答纠偏，最后输出定义、区别、易错点和记忆总结。

自测结论：通过。该 skill 会引导用户自己形成理解，不会一上来长篇灌输。

### 9. `009-tampermonkey-from-scratch`

需求理解：用于从零设计、编写、安装并验证油猴脚本。

调用场景：用户要一个脚本自动填写后台登录页。

按 skill 期望输出：

- 先理解目标页面、手动操作流程、权限和触发方式。
- 设计 `@match`、选择器、注入时机和失败回退。
- 安装脚本后用测试账号打开真实登录页验证填充、提交按钮状态、错误提示和登录后不重复填充。

自测结论：通过。该 skill 明确要求真实页面或复刻页面验证，不会只给一段 userscript 代码。

### 10. `010-learning-summary-lite`

需求理解：用于把指定学习材料总结成精简、易懂、可吸收的版本。

调用场景：用户提供 OAuth 章节，希望“精简易懂总结”。

按 skill 期望输出：

- 先确认范围、读者水平、重点和输出偏好。
- 输出核心结构、关键术语、通俗解释、易错点。
- 用一个“第三方登录”小场景验证读者是否能区分授权码、access token、refresh token。

自测结论：通过。该 skill 会用小样例检查总结是否可理解，而不是只输出摘要。

### 11. `011-understand-code-before-change`

需求理解：用于已有代码库需求实现，先理解代码再动手。

调用场景：用户要求修复“登录后跳转错误”。

按 skill 期望输出：

- 先读路由守卫、登录表单、session 存储、后端响应和已有测试。
- 再做最小改动。
- 用测试账号从受保护页面跳转登录，验证登录后回到原页面、刷新状态保持、退出后访问受保护页面会重定向。

自测结论：通过。该 skill 会强制代码理解和真实用户路径验证。

### 12. `012-codex-control-multisession`

需求理解：用于总控会话拆分多 session，并要求每个 session 带验证责任。

调用场景：用户要做后台系统，拆成登录、订单、报表三个 session。

按 skill 期望输出：

- 先形成需求镜像。
- 给 session_1/session_2/session_3 分别写清输入、任务、产物、禁止事项。
- 每个 session 都要求真实场景自测，例如登录 session 要预置测试账号并验证登录/登出/session。

自测结论：通过。该 skill 不只是拆任务，还会把验收和回传格式写进每个交接 Prompt。

### 13. `013-directory-overview`

需求理解：用于通过目录结构先建立项目整体认知。

调用场景：用户给一个仓库目录树，希望知道从哪看。

按 skill 期望输出：

- 根据目录推断项目类型、模块边界、入口和阅读顺序。
- 标注高置信推断和低置信猜测。
- 抽查少量关键文件验证推断。

自测结论：通过。该 skill 会避免只凭目录武断下结论，要求关键文件抽查。

### 14. `014-langchain-agent-planner`

需求理解：用于正式创建 LangChain Agent 前澄清任务、工具、Memory、输入输出、失败处理和评估样例。

调用场景：用户要做客服知识库 Agent。

按 skill 期望输出：

- 判断是否真的需要 Agent。
- 明确工具清单、检索数据源、输入输出 schema、Memory 策略、失败处理。
- 用真实问题“如何重置密码”验证检索、引用来源、回答质量和找不到答案时的人工升级。

自测结论：通过。该 skill 会把 Agent 需求收敛到可实现和可评估的方案。

### 15. `015-langgraph-agent-planner`

需求理解：用于设计 LangGraph 工作流前澄清状态、节点、边、循环、人工中断和恢复。

调用场景：用户要做报销审批 Agent。

按 skill 期望输出：

- 定义 State schema、节点职责、边条件和终止状态。
- 识别死循环、状态膨胀、恢复和人工审批风险。
- 用 1200 元报销单跑提交、规则检查、经理审批、财务复核、驳回重提路径。

自测结论：通过。该 skill 会验证完整图路径，而不是只测单个 node 函数。

### 16. `016-multi-agent-architect`

需求理解：用于多 Agent 架构设计前判断是否需要多 Agent，并设计角色、通信、调度和评估。

调用场景：用户要做客服工单多 Agent 系统。

按 skill 期望输出：

- 先判断单 Agent 或普通工作流是否足够。
- 定义分类 Agent、账号 Agent、订单 Agent、总结 Agent 的职责和工具权限。
- 用“登录不了且订单看不到”的工单剧本模拟多 Agent 协作，验证路由、信息传递、冲突处理和最终回复。

自测结论：通过。该 skill 能避免为了多 Agent 而多 Agent，并要求协作剧本验证。

## 发现并修复的问题

- 参考 Browser Use 后，补充了 `references/operating-contract.md`，把硬性规则、操作配方、失败处理和证据标准写进每个自建 skill。
- 修复了插入过程中出现的字面量换行标记。
- 保持目录编号便于查找，同时保留 `SKILL.md` 中无序号的 `name`，便于实际调用。
