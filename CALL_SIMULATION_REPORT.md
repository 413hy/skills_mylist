# 自建 skills 调用式自测报告

日期：2026-06-06

## 说明

这份报告补充上一份 `SELF_TEST_REPORT.md`。

严格说，本轮测试不是 Codex 运行时里输入 `$skill-name` 后由系统自动注入 skill 的真实触发测试；当前会话没有把本目录临时注册为 Codex skills 并触发 `$skill` 的工具。  
本轮做的是“调用式模拟”：逐个读取自建 skill 的 `SKILL.md` 和 references，用代表性用户输入实际生成一段应答样例，再检查输出是否符合该 skill 的目标、输出契约和真实场景自测门槛。

判定标准：

- 是否先理解需求，而不是直接乱做。
- 是否输出该 skill 应该产出的内容形态。
- 涉及最终产品、代码、demo、脚本、Agent、PR 或同步结果时，是否要求真实场景自测。
- 是否避免“只测接口通不通”这种低质量验证。
- 是否有明确的下一步、缺口或阻塞说明。

## 调用式自测结果

### 1. `001-pr-check-commit-pr`

调用输入：

> 检查当前登录模块改动，没问题就提交并发 PR。

实际应答样例：

> 理解镜像：本次要提交的是登录模块改动。提交前我需要确认 git diff、目标分支、关联需求、测试命令和登录功能验收路径。  
> 执行前检查：我会先检查工作区和 diff，再跑 lint/test/build。  
> 场景自测要求：使用测试账号从登录页验证正确登录、错误密码、刷新后会话保持、退出登录、受保护页面访问。通过后再提交、推送并准备 PR 描述。  
> 需要补齐：如果仓库没有测试账号或启动命令，我会先从文档中找；找不到会列为阻塞。

检查结论：符合。输出没有直接提交，而是先建立检查和真实登录场景自测门槛。

### 2. `002-user-story-from-materials`

调用输入：

> 根据材料“运营人员要能登录后台管理订单，失败时提示原因”生成 User Story。

实际应答样例：

> User Story：作为运营人员，我希望使用账号密码登录后台，以便安全地进入订单管理页面。  
> 验收标准：正确账号密码可登录并进入订单页；密码错误展示明确提示；账号禁用时提示不可登录；未登录访问订单页会跳转登录页；登录后刷新仍保持会话。  
> 场景验证样例：使用 `qa_ops@example.com` 登录后台，验证成功路径、错误密码、禁用账号、会话过期。  
> 待确认：是否需要验证码、SSO、多因素认证、登录失败锁定策略。

检查结论：符合。输出不是复述材料，而是补齐了故事、验收标准和可执行测试样例。

### 3. `003-sync-doc-to-target-system`

调用输入：

> 把登录 User Story 同步到 Jira，并确认同步结果。

实际应答样例：

> 理解镜像：源对象是登录 User Story，目标系统是 Jira。同步前需要目标项目、issue 类型、字段映射和权限。  
> 同步计划：创建/更新 Jira issue，写入标题、描述、验收标准、优先级、Assignee、关联文档链接。  
> 同步后验证：打开或查询目标 issue，逐项核对字段，不以 API 成功响应作为唯一证据。  
> 交付输出：目标链接、字段核对表、差异项、无法同步字段和原因。

检查结论：符合。强调目标系统回查，符合同步类 skill 的核心目的。

### 4. `004-cross-system-alignment`

调用输入：

> 需求文档说登录失败 5 次锁定账号，代码和 Jira 可能不一致，帮我对齐。

实际应答样例：

> 对齐来源：需求文档、代码实现、Jira 状态、实际系统行为。  
> 权威顺序：先按需求文档，实际行为用于验证实现，Jira 反映任务状态。  
> 真实场景验证：用测试账号连续输错密码 5 次，观察是否锁定、提示是否正确、Jira 是否应保持 Done。  
> 输出：差异表、影响评估、建议修正项、需要产品确认的问题。

检查结论：符合。输出要求实际操作验证，而不是只比较文字。

### 5. `005-step-by-step-guide`

调用输入：

> 登录失败了，你一步一步带我排查。

实际应答样例：

> 下一步：打开浏览器开发者工具的 Network 面板，然后尝试登录一次。  
> 需要我反馈：登录请求的状态码、响应内容、请求 URL，以及页面上显示的错误提示。

检查结论：符合。一次只给一个动作，并明确需要用户反馈什么。

### 6. `006-ai-summary-handoff`

调用输入：

> 这个登录模块做到一半了，帮我总结给另一个 AI 接着做。

实际应答样例：

> 核心目标：继续完成登录模块并通过真实场景自测。  
> 当前进度：已完成登录页和基础接口；未确认会话保持、退出登录、受保护路由。  
> 下一位 AI 的任务：先读取相关文件和测试命令，再用测试账号验证成功登录、失败登录、刷新、退出和权限跳转。  
> 交接 Prompt：包含目标、已知文件、已跑测试、失败日志、待办、验收标准和边界。

检查结论：符合。输出是可执行交接，不是泛泛总结。

### 7. `007-clarify-before-demo`

调用输入：

> 帮我做一个后台管理 demo，要有登录。

实际应答样例：

> 下一步澄清问题：这个后台 demo 的核心业务对象是什么，例如订单、用户、库存，还是报表？  
> 说明：确认业务对象后，我会继续补齐核心闭环；在需求镜像确认前不直接生成 demo。

检查结论：符合。先问一个关键问题，不直接脑补完整后台。

### 8. `008-socratic-concept-learning`

调用输入：

> 带我理解 Cookie、Session、JWT 的区别。

实际应答样例：

> 先建立整体地图：这三个概念都和“服务端如何识别用户身份状态”有关。Cookie 更像浏览器携带的小票，Session 更像服务端保存的座位号，JWT 更像带签名的通行证。  
> 问题：如果用户第二次请求不带任何标识，服务端还能知道他是谁吗？

检查结论：符合。输出不是长篇讲解，而是先给低术语地图，再提出一个诊断问题。

### 9. `009-tampermonkey-from-scratch`

调用输入：

> 写个油猴脚本，自动填写后台登录页。

实际应答样例：

> 理解镜像：目标是自动填写登录页，而不是自动绕过登录。  
> 需要信息：登录页 URL、账号字段选择器、密码字段选择器、是否自动点击提交、测试账号。  
> 实施计划：设计 `@match`、等待 DOM、稳定选择器、避免重复填充、错误提示不遮挡。  
> 场景自测：安装脚本后打开真实登录页，用测试账号验证填充、提交按钮状态、错误提示、登录后不重复填充。

检查结论：符合。输出包含页面分析和安装后真实验证，不只是代码片段。

### 10. `010-learning-summary-lite`

调用输入：

> 帮我精简总结 OAuth 授权码流程，适合初学者。

实际应答样例：

> 理解镜像：目标是面向初学者解释 OAuth 授权码流程，重点是流程和角色，不展开底层协议细节。  
> 精简总结：用户先被引导到授权服务器登录并授权，应用拿到授权码，再用授权码换 access token，之后用 token 调 API。  
> 小样例自测：用户点击“用 GitHub 登录”时，授权码出现在哪一步？access token 应该给浏览器保存，还是由后端保存？

检查结论：符合。输出包含学习内容和理解检查样例。

### 11. `011-understand-code-before-change`

调用输入：

> 修一下登录后跳转错误的问题。

实际应答样例：

> 理解镜像：这是已有代码库中的登录跳转 bug，不能直接改。  
> 先读代码：路由守卫、登录表单、session 存储、后端登录响应、已有测试。  
> 修改后自测：从受保护页面进入登录页，使用测试账号登录后应回到原页面；刷新保持登录；退出后再次访问受保护页面应跳回登录页。  
> 交付时说明：改动文件、测试命令、真实场景自测结果和未覆盖风险。

检查结论：符合。先理解代码再改，并要求用户路径验证。

### 12. `012-codex-control-multisession`

调用输入：

> 这个后台项目很大，你作为总控拆成几个 Codex 会话。

实际应答样例：

> 需求镜像：后台项目可拆成登录权限、订单管理、报表看板三个 session。  
> session_1：负责登录权限，必须预置测试账号并验证登录、错误密码、会话保持、退出登录。  
> session_2：负责订单管理，必须用样例订单验证新增、查询、状态变更。  
> session_3：负责报表看板，必须用样例数据验证筛选、汇总和空状态。  
> 每个 session 回传：改动范围、测试数据、真实场景自测结果、阻塞项。

检查结论：符合。拆分结果包含每个 session 的验证责任。

### 13. `013-directory-overview`

调用输入：

> 这是一个仓库目录树，帮我通过目录看大概思路。

实际应答样例：

> 初步判断：`src/routes` 是页面入口，`src/auth` 可能负责认证，`api/session` 可能负责会话接口。  
> 置信度：仅凭目录不能断言功能完整，需要抽查路由守卫、登录页和 session API。  
> 建议阅读顺序：README、入口路由、auth 模块、API 客户端、测试目录。  
> 下一步：读取 2-3 个关键文件验证目录推断。

检查结论：符合。输出区分推断和事实，并要求抽查验证。

### 14. `014-langchain-agent-planner`

调用输入：

> 帮我规划一个客服知识库 LangChain Agent。

实际应答样例：

> 需求镜像：目标是客服知识库问答，不一定一开始就需要复杂 Agent；需要确认工具、知识库、引用来源、兜底策略。  
> 架构草案：Retriever 工具、答案生成、来源引用、低置信度转人工。  
> 评估样例：输入“如何重置密码”，Agent 应检索密码重置文档、给出步骤、附来源；找不到答案时升级人工。  
> 待确认：知识库格式、更新频率、是否需要会话记忆。

检查结论：符合。输出可评估，不只是泛泛“用 LangChain”。

### 15. `015-langgraph-agent-planner`

调用输入：

> 规划一个报销审批 LangGraph Agent。

实际应答样例：

> State 草案：申请人、金额、发票、审批状态、驳回原因、历史记录。  
> 节点草案：提交校验、规则检查、经理审批、财务复核、通知、结束。  
> 边条件：金额阈值、资料缺失、审批通过/驳回、重提。  
> 场景自测：用 1200 元报销单跑提交、规则检查、经理审批、财务复核、驳回重提，验证状态流转和终止条件。

检查结论：符合。输出聚焦图状态和完整路径验证。

### 16. `016-multi-agent-architect`

调用输入：

> 设计一个客服工单多 Agent 架构。

实际应答样例：

> 先判断必要性：如果只是简单 FAQ，单 Agent + 检索足够；如果要处理账号、订单、退款多域问题，多 Agent 有价值。  
> 角色草案：分类 Agent、账号 Agent、订单 Agent、退款 Agent、总结 Agent。  
> 通信协议：每个 Agent 输出事实、证据、置信度、下一步建议。  
> 协作剧本自测：用户说“登录不了且订单看不到”，验证分类、账号排查、订单排查、冲突合并和最终客服回复。

检查结论：符合。输出先判断是否需要多 Agent，并要求协作剧本验证。

## 总结

本轮调用式模拟中，16 个自建 skill 的输入输出均符合我对它们需求目标的理解。  
仍需说明：这不是 Codex 运行时 `$skill-name` 自动触发测试；真正的运行时触发需要把这些目录安装到 Codex skills 目录并重启/重新加载后再测。

